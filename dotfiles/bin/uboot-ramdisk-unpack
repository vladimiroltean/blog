#!/bin/bash

IMAGE_FILENAME=$1
ARCHIVE_FILENAME=${IMAGE_FILENAME%.u-boot*}
FILESYSTEM_FILENAME=${ARCHIVE_FILENAME%.gz*}
dd if=$IMAGE_FILENAME of=$ARCHIVE_FILENAME bs=64 skip=1
zcat $ARCHIVE_FILENAME > $FILESYSTEM_FILENAME
